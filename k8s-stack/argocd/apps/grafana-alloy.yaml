apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: grafana-alloy
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default
  sources:
    - chart: alloy 
      repoURL: https://grafana.github.io/helm-charts
      targetRevision: 1.1.1
      helm:
        valueFiles:
          - $values/k8s-stack/values/grafana-alloy/values.yaml
    - repoURL: 'https://github.com/otkisyan/medical-center-api.git'
      targetRevision: master 
      ref: values
  destination:
    name: "in-cluster"
    namespace: default
  project: default
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
